#include <bits/stdc++.h>
using namespace std;
int main()
{

    int a, b, n, m;
    cin >> n >> m >> a >> b;
    int bol = n * a;
    int suri = m * b;
    cout << suri + bol << endl;

    return 0;
}